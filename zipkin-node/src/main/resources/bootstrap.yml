server:
  port: 9966

spring:
  application:
    name: zipkinnode
    data:
    mongodb:
      host: localhost
      port: 27017
      database: zipkinnode 
    profiles:
    active:
      default
      
zipkin:
  storage:
    type: mem      
      
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://192.168.99.100:8761/eureka}
  cloud:
    config:
      uri: http://192.168.99.100:8888