server:
  port: 8686

spring:
  application:
    name: fullnode
    profiles:
    active:
      default
  data:
    mongodb:
      host: localhost
      port: 27017
      database: fullnode

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://192.168.99.100:8761/eureka}      